# Core Logic & CLI
typer>=0.9.0
rich>=13.0.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# Static Analysis Tools (The "Builders")
radon>=6.0.0
lizard>=1.17.0

# Git History Analysis
GitPython>=3.1.0

# AI Integration (try new API, fallback to old if needed)
google-genai>=0.2.0
google-generativeai>=0.3.0  # Keep as fallback

